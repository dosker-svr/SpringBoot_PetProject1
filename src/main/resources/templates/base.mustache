<!DOCTYPE HTML>
<html>
<body>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" /> {{! любой запрос на сервер не сопровождающийся этим
                                                                         токеном будет игнорироваться сервером}}
        <input type="submit" value="Sign Out"/>
    </form> {{! только после авторизации, пользователь сможет увидеть остальную часть страницы ∨∨∨}}
    <div>
        <form method="post">                       {{! форма ввода данных в db}}
            <input type="text" name="text" placeholder="Введите сообщение" />
            <input type="text" name="tag" placeholder="Тэг" />
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" /> {{! любой запрос на сервер не сопровождающийся этим
                                                                         токеном будет игнорироваться сервером}}
            <button type="submit">Добавить</button>
        </form>
    </div>
    <div>Список сообщений</div>
    <form method="post" action="filter">           {{! форма фильтрации данных в db}}
        <input type="text" name="filter" />
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" /> {{! любой запрос на сервер не сопровождающийся этим
                                                                         токеном будет игнорироваться сервером}}
        <button type="submit">Найти</button>
    </form>
    {{#messages}}                 {{!списки в mustache выглядят вот так ∨∨∨}}
    <div>
        <b>{{id}}</b>
        <span>{{text}}</span>
        <i>{{tag}}</i>
    </div>
     {{/messages}}                {{!списки в mustache выглядят вот так ^^^}}
</body>
</html>